package DSA.CODE;

public class sec10_TRIE {
    public static void main(String[] args) {
        // Trie is a tree like data structure
        // Trie is used to store strings
        // Trie is also called prefix tree or digital tree
        // Trie is used to search strings in O(L) time where L is the length of the string
        // Trie is also used to store strings which can be represented as a prefix of another string
        // every node links to  next character in the string
        // every node keep track of end of string
    }
}
